# Memory Structure
- In MIPS, memory is byte addressed.
- In simulator, memory managed by word matching the size of an instruction (32 bit)

## Load / Store
- Can load / store 32 bit words from memory
